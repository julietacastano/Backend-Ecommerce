<h1>Products</h1>
<div>
    {{#if productsNotEmpty}}
    <ul>
        {{#each products}}
        <li>{{this.title}}</li>
        {{/each}}
    </ul>
    {{else}}
    <h3>There are no product available</h3>
    {{/if}}
</div>
<script src="/static/js/products.js"></script>